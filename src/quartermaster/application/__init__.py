"""Application layer: command handlers, the transaction envelope, and the
repository ports (Protocols) that adapters implement.

Depends only on :mod:`quartermaster.domain`; it never imports concrete adapters.
"""
